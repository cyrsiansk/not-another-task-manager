# Tasks - list, filter and limit

## List tasks with status filter and limit
Tags: tasks, list, filter

* Generate random credentials
* Register user via API (expect success)
* Login with credentials (expect success)
* Create task with title "t1" and status "created"
* Create task with title "t2" and status "in_progress"
* Create task with title "t3" and status "done"
* Create task with title "filler" and status "done"
* List tasks with status "in_progress" and check only tasks with that status are returned
* List tasks with limit "2" and check no more than that limit is returned
* List tasks with limit "3" and check no more than that limit is returned